services:
  sensor-hub:
    build:
      context: ..
      dockerfile: docker/sensor-hub.dockerfile
    ports:
      - "8080:8080"
    volumes:
      - sensor-hub-data:/app/data
volumes:
  sensor-hub-data: